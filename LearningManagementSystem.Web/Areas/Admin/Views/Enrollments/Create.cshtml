@model CreateEnrollmentDto
@{
    ViewData["Title"] = "New Enrollment";
}

@section Styles {
<style>
    .form-card {
        background: #fff;
        border-radius: var(--lms-radius, 16px);
        padding: 2.25rem;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
        border: 1px solid rgba(0, 0, 0, 0.05);
    }

    .form-header {
        border-bottom: 1px solid #edf2f7;
        padding-bottom: 1.25rem;
        margin-bottom: 1.75rem;
    }

    .form-header h2 {
        font-weight: 700;
        font-size: 1.5rem;
        color: var(--lms-dark, #1c1d1f);
        margin-bottom: 0.25rem;
    }

    .form-header p {
        color: var(--lms-muted, #6a6f73);
        font-size: 0.9rem;
        margin: 0;
    }

    .form-label {
        font-weight: 600;
        font-size: 0.88rem;
        color: var(--lms-dark, #1c1d1f);
        margin-bottom: 0.4rem;
    }

    .form-control,
    .form-select {
        border-radius: 10px;
        padding: 0.65rem 1rem;
        font-size: 0.92rem;
        border: 1px solid #cbd5e1;
        transition: var(--lms-transition, all 0.2s ease);
    }

    .form-control:focus,
    .form-select:focus {
        border-color: var(--lms-primary, #5624d0);
        box-shadow: 0 0 0 3px rgba(86, 36, 208, 0.15);
    }

    .btn-submit {
        background: var(--lms-gradient, linear-gradient(135deg, #5624d0, #7c3aed));
        color: #fff;
        font-weight: 600;
        border-radius: 10px;
        padding: 0.7rem 1.8rem;
        border: none;
        box-shadow: 0 4px 14px rgba(86, 36, 208, 0.25);
        transition: var(--lms-transition, all 0.3s ease);
    }

    .btn-submit:hover {
        opacity: 0.92;
        color: #fff;
        transform: translateY(-2px);
    }

    .btn-cancel {
        background: #f1f5f9;
        color: var(--lms-muted, #6a6f73);
        font-weight: 600;
        border-radius: 10px;
        padding: 0.7rem 1.5rem;
        border: none;
        text-decoration: none;
        transition: var(--lms-transition, all 0.2s ease);
        display: inline-flex;
        align-items: center;
    }

    .btn-cancel:hover {
        background: #e2e8f0;
        color: var(--lms-dark, #1c1d1f);
    }
</style>
}

<div class="container py-4">
    <nav aria-label="breadcrumb" class="mb-3">
        <ol class="breadcrumb" style="font-size: 0.85rem;">
            <li class="breadcrumb-item"><a asp-area="Admin" asp-controller="Home" asp-action="Index" class="text-decoration-none text-muted">Dashboard</a></li>
            <li class="breadcrumb-item"><a asp-area="Admin" asp-controller="Enrollments" asp-action="Index" class="text-decoration-none text-muted">Enrollments</a></li>
            <li class="breadcrumb-item active text-primary fw-semibold" aria-current="page">New Enrollment</li>
        </ol>
    </nav>

    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="form-card">
                <div class="form-header">
                    <h2><i class="bi bi-person-plus-fill text-primary me-2"></i>Enroll Student</h2>
                    <p>Assign a student to a course and initialize their enrollment progress.</p>
                </div>

                <form asp-area="Admin" asp-controller="Enrollments" asp-action="Create" method="post">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-4" role="alert"></div>

                    <div class="row g-4">
                        <div class="col-12">
                            <label asp-for="StudentId" class="form-label"></label>
                            <select asp-for="StudentId" asp-items="ViewBag.StudentId" class="form-select">
                                <option value="">-- Select Student --</option>
                            </select>
                            <span asp-validation-for="StudentId" class="text-danger small"></span>
                        </div>

                        <div class="col-12">
                            <label asp-for="CourseId" class="form-label"></label>
                            <select asp-for="CourseId" asp-items="ViewBag.CourseId" class="form-select">
                                <option value="">-- Select Course --</option>
                            </select>
                            <span asp-validation-for="CourseId" class="text-danger small"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="EnrollmentDate" class="form-label"></label>
                            <input asp-for="EnrollmentDate" class="form-control" type="date" />
                            <span asp-validation-for="EnrollmentDate" class="text-danger small"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="Progress" class="form-label"></label>
                            <div class="input-group">
                                <input asp-for="Progress" class="form-control" type="number" min="0" max="100" step="1" value="0" />
                                <span class="input-group-text bg-light text-muted">%</span>
                            </div>
                            <span asp-validation-for="Progress" class="text-danger small"></span>
                        </div>

                        <div class="col-12 mt-4 pt-3 border-top d-flex align-items-center justify-content-end gap-2">
                            <a asp-area="Admin" asp-controller="Enrollments" asp-action="Index" class="btn btn-cancel">
                                Cancel
                            </a>
                            <button type="submit" class="btn btn-submit">
                                <i class="bi bi-check-lg me-1"></i> Save Enrollment
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
